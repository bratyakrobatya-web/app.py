# Core dependencies (pinned versions for security)
streamlit==1.51.0
rapidfuzz==3.14.3
openpyxl==3.1.5
pandas==2.3.3
requests==2.32.5
Pillow==12.0.0

# System dependencies (minimum versions for security)
# These are typically pre-installed but must be upgraded for security:
# cryptography>=46.0.3  # Fixes CVE-2024-26130, CVE-2023-50782, CVE-2024-0727
# setuptools>=80.9.0    # Fixes CVE-2025-47273, CVE-2024-6345
# pip>=25.3             # Fixes CVE-2025-8869

# Testing dependencies
pytest==9.0.1
pytest-cov==7.0.0

# Security scanning tools (development only)
# Uncomment to install:
# bandit==1.7.10
# safety==3.2.11
